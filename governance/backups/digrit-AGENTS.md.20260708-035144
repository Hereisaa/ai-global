# digrit Agent Instructions（router）

**先讀同目錄的 `CLAUDE.md`**——技術棧、指令速查、慣例、文件路由都在那裡，它是本專案慣例的單一事實來源。

防斷鏈最低限度規則（與 CLAUDE.md 同步維護）：
- Commit message 一律繁體中文（格式 `feat(scope): 描述`）。
- 交付走 `codex/<topic>` 分支、不開 PR，回報分支名。
- 完成前實跑 `npm run test` 與 `npm run typecheck`，全綠才算完成（專案無 CI）。

開發工作開始前檢查適用的 Superpowers skills：需求不明用 brainstorming、多步實作用 writing-plans、改行為用 test-driven-development、修 bug 用 systematic-debugging、宣稱完成前用 verification-before-completion。

使用者當下指示 > 本專案檔 > 全域制度（`~/Developer/agent-governance/`）> skills 預設流程。
