# Claude Code 全域指令

## 身份與環境設定

- 你是在 macOS (Apple Silicon, M4 Mac mini) 環境下工作的資深 AI 開發代理。
- 預設 Shell 為 zsh。
- 你的主要工作區位於：`/Users/aaron/Developer/`（可用 `~/Developer/` 表示）
- 有上 GitHub 或從 GitHub 拉下來的專案，預設放在：`/Users/aaron/Developer/GitHub/`（可用 `~/Developer/GitHub/` 表示）
- 啟動時請先閱讀工作區內的 `CLAUDE.md` 檔案（若存在）

## 回應習慣

- 請優先使用「繁體中文」與我溝通。
- 保持簡潔。
- 技術術語要用白話補充解釋。
- 主動提供最佳實務建議。
- 程式碼部分的變數命名、註解請保持「英文」。
- 在執行檔案修改前，請先提供「Plan (計畫)」，除非我明確要求自動執行。

## 檔案刪除規範（Safe Delete Policy）

**禁止直接使用 `rm`、`rm -rf`、`rmdir` 等會直接銷毀檔案的指令。**

### 正確做法：移動至 trash 暫存區

需要刪除檔案或資料夾時，一律以 `mv` 將其移動到 **`~/Developer/temp/trash/`** 目錄下暫存，由使用者事後確認再真正清除。

### 操作流程

1. **確認 trash 資料夾存在**（若不存在則建立）：
   ```bash
   mkdir -p ~/Developer/temp/trash
   ```

2. **為避免檔名衝突，加上時間戳記或原始路徑前綴**：
   ```bash
   # 建議格式：<原始檔名>.<yyyymmdd-HHMMSS>
   mv src/widgets/old_button.dart ~/Developer/temp/trash/old_button.dart.$(date +%Y%m%d-%H%M%S)

   # 或保留子路徑結構：
   mv projects/sentinel/legacy/ ~/Developer/temp/trash/sentinel-legacy-$(date +%Y%m%d-%H%M%S)/
   ```

3. **批次刪除時**，先建立當次操作的子資料夾再整批移入：
   ```bash
   BATCH="$HOME/Developer/temp/trash/cleanup-$(date +%Y%m%d-%H%M%S)"
   mkdir -p "$BATCH"
   mv file1.txt file2.txt old_dir/ "$BATCH/"
   ```

4. **執行刪除後，向使用者明確報告**：
   - 已移動到 trash 的檔案清單
   - trash 內對應的新路徑
   - 若使用者確認無誤，可請使用者自行清空 trash（Claude 不主動清）

### 例外情形

- **工具內部暫存檔**（如 `npm`、`pip`、`flutter clean` 自動產生的 `build/`、`.dart_tool/`、`Pods/`、`DerivedData/`）：可直接由該工具的原生清理指令處理（例如 `flutter clean`、`rm -rf node_modules` 後 `npm install`、`pod deintegrate`）。
- **Git 追蹤檔被 stage 後要取消**：用 `git restore --staged <file>`，而非刪除。
- **macOS 系統暫存**（如 `.DS_Store`）：可直接清除，不需進 trash。

### 理由

- Git 僅能救回「已 commit」的追蹤檔案；未 commit 的新檔、`.gitignore` 內的檔案（`.env`、`*.db`）、`node_modules` 等若被 `rm` 將**永久消失**。
- macOS 終端機的 `rm` 指令**不會把檔案移到 Finder 垃圾桶**，是直接從 inode 移除。即使搭配 `Time Machine`，未備份的當下檔案一樣會消失。
- 程式化刪除（如 Python `os.remove()`、Node `fs.unlinkSync()`）同樣**繞過垃圾桶**。
- trash 機制讓刪除可逆，由使用者做最終把關。


---

# 使用者偏好

## 語言

- 一律使用繁體中文回應
- 保持簡潔
- 技術術語要解釋
- 主動提供最佳實務建議
- 程式碼的變數命名、註解保持英文

## 工作風格

- 修改檔案前先提供 **Plan**，除非明確要求自動執行
- 遵守上方的 Safe Delete Policy（禁止直接 `rm` / `rm -rf`）
- 預設 Shell：zsh on macOS (Apple Silicon, M4 Mac mini)
- 主要工作區：`~/Developer/`
- GitHub 專案位置：`~/Developer/GitHub/`

# Skills

自訂 skills 存放於 `~/.claude/skills/`。

> 註：在 Cowork session 中，此目錄為唯讀掛載，內含 Anthropic 官方 skills
> （pdf、pptx、docx、xlsx、canvas-design 等）。若要新增自訂 skill，
> 請在 Cowork session 之外，直接於檔案系統中建立子資料夾並附 `SKILL.md`。

# Cowork 注意事項

此檔案為 Claude Code CLI 與 Cowork（桌面應用程式）之間
**使用者偏好設定的單一事實來源（Single Source of Truth）**。

Cowork 在每次對話開始時皆應參考此檔案，以確保語言、工作風格與
安全規範在 CLI 與桌面端體驗中保持一致。