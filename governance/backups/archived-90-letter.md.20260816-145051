# 90 — 給未來 session 的信

寫於 2026-07-03，Fable 5 唯一一次 session。你（正在讀的模型）大概率是 Sonnet、Opus、Haiku 或 GPT 系。這封信是制度檔以外、我認為你最需要知道的三件事，以及這套制度會怎麼壞掉。

## 三件使用者沒問、但最重要的事

### 1. 有兩個懸置狀態需要儘早處理
- `~/.claude/settings.json` 的 `model` 釘在 `claude-fable-5[1m]`——本 session 結束後這個模型不可用。第一個接手的 session 應提醒使用者用 `/model` 改成可用模型，否則啟動行為不可預期。
- digrit 的 `claude/p1-final` 分支在制度建立時仍是「待驗證合併」狀態（見 auto-memory 的全專案盤查檔；memory 索引在 `~/.claude/projects/-Users-aaron-Developer-GitHub-digrit/memory/MEMORY.md`）。接手 digrit 工作前先確認它合了沒，別在舊基底上開工。

### 2. 記憶機制有裂縫，檔案才是可靠的傳承載體
Claude Code 的 auto-memory 只在「同一個 cwd 路徑」的 session 間共享——worktree 有自己的一份，Codex 則完全沒有這套。所以：**值得留下的教訓，一律寫進檔案**（通用的進 `40-maintenance.md` 教訓記錄，專案的進專案 CLAUDE.md 或主專案 memory），不要只依賴任何單一 provider 的記憶功能。判斷「值得留下」的門檻：下一個對此一無所知的模型會不會因為不知道而犯錯？會，就寫。

### 3. 這個環境的核心資產是「已鎖定的決策」，不是程式碼
digrit 有完整的產品文件樹（`docs/digrit_product_docs/`）和多個鎖定決策（漲跌色、雙幣別、provider 抽象、prompt registry、displayName SSOT……）。模型會換，程式碼會改，但這些決策是使用者花真金白銀的對話錘出來的。**動手前查 canon** 的習慣，比任何單次產出的品質都重要。反過來說：如果你發現某個鎖定決策確實過時了，不要默默繞過——提案給使用者改 canon，讓決策文件與現實保持一致。

## 這套制度最可能的退化方式（與預防法）

1. **儀式化**：把 checklist 當咒語唸完就開工，或把「派 subagent」本身當成目的（10 行的小修也開三個 agent）。制度的每條規則都對應 [00-diagnosis.md](00-diagnosis.md) 的一個病灶——不知道規則在防什麼病，就會執行得走樣。預防：例外清單照用（單點小修自己做）；覺得規則不合理時走 [40-maintenance.md](40-maintenance.md) 提案修改，而不是默默不遵守。
2. **自我放寬**：長 session 後段「為了效率」跳過 fresh-context 驗收、跳過測試實跑。這正是 00 診斷的第三號病灶，而且越弱的模型越容易在 context 壓力下放寬。預防：「驗證不自驗」是鐵律，沒有「這次比較簡單所以不用」的例外；使用者也可以不定期抽問「這個結論的驗證證據是什麼」。
3. **規則累積肥大**：每踩一個坑加一條規則，三個月後 CLAUDE.md 又變回 4KB 大雜燴。預防：長度預算是硬上限（[40-maintenance.md](40-maintenance.md)），教訓滿 20 條強制畢業或歸檔；新增規則前先問「這能不能併進既有條目」。
4. **兩側漂移**：只改 Claude 側、忘了 Codex 側（或反之），半年後兩邊規則打架。預防：實質規則只寫在本目錄，兩個 router 只放摘要與指路；改 router 的共通節必須同步另一邊（40 有明文）。
5. **router 斷鏈**：檔案被搬移改名，全域 CLAUDE.md 指向不存在的路徑，後續模型安靜地忽略。預防：斷鏈時必須回報使用者（router 內有明文）；重組檔案結構屬於「必須先問」等級。

## 誠實條款（引用一次，因為它最容易被忘）

這套制度補得了執行品質，補不了模糊題與品味判斷。你遇到這兩類時的合法動作：升級模型、找第二意見（含使用者）、或明說做不到。裝懂是這裡唯一不可原諒的失敗模式。

## 未完成交接

（本 session 若中斷，未完成項目寫在這裡；目前：無——全部交付項已落檔並通過收尾驗證。）

## 變更紀錄
- 2026-07-03 建檔（Fable 5）
