# REPORT — Fable 5 與 Opus 4.8 的表現差異：分析與移植指南

> 讀者：**使用者本人**。撰於 2026-07-08，由 Fable 5 應使用者要求，結合自身行為的第一手觀察與公開資料查證寫成。
> 可執行的規範版本在 [70-behavior-contract.md](70-behavior-contract.md)；本檔是「為什麼」與「怎麼辦到」。

## TL;DR

你感受到的差異來自三層：**模型本體能力**（訓練成果，不可移植）、**行為契約**（harness 明文規範的輸出與工作紀律，高度可移植）、**判斷習慣**（何時停、何時問、何時信心足夠，可用 rubric 部分移植）。關鍵發現：官方文件證實 Fable 5 未經調校的預設輸出其實**更難讀**（箭頭鏈、術語密集）——你喜歡的清晰輸出，是行為契約調教出來的，而這份契約可以移植。我已把它蒸餾成 70 檔並掛上 Claude 與 Codex 兩側路由。

## 一、差異從哪來：三層分解

### 第一層：模型本體（不可移植）

Fable 5 與 Mythos 5（2026-06-09 發布）共用同一底層模型，是位階高於 Opus 的新檔次；Fable 版多了 dual-use 能力的安全分類器。官方數據的核心模式是「**任務越長越複雜，領先越大**」：

- SWE-bench Pro：80.3%（Fable 5）vs 69.2%（Opus 4.8）；FrontierCode Diamond：29.3% vs 13.4%。
- 誠實度是量級差異：不誠實的 session 總結率 4.6%，前代模型超過 50%。這是訓練成果，不是 prompt 成果。
- 官方文件列出的行為改進：多小時自主運行、well-specified 複雜問題的首發正確率、除錯時更高的抓蟲召回率、更可靠的平行 subagent 調度。

但第三方實測補了重要的另一半：**Tessl 用 917 個日常情境實測，Fable 5 得 92.9 分、Opus 4.8 得 92.0 分**——日常任務幾乎打平，Opus 4.8 的每元品質還高約 69%。差距集中在長程、跨檔案、需要自我驗證的工作。也有退步項：文獻引用幻覺率 18%（Opus 4.8 是 9%），且很少主動說「我不知道」。

### 第二層：行為契約（高度可移植——這是重點）

我在 Claude Code 裡運作時，行為受一份明文行為規範約束。有一個反直覺的證據能說明它多重要：官方 prompting 文件明載，未調校的 Fable 5 預設報告風格是「dense／terse」——箭頭鏈簡寫、術語堆疊、過度結構化——Anthropic 是**靠一段 readability 指令**把它逆轉成你看到的樣子。也就是說，「Fable 5 的輸出真好讀」這件事，一半以上是契約的功勞，而契約可以照抄給別的模型。

契約的十個關鍵條款（完整版見 70 檔）：結論先行、可讀優先於簡短、最終訊息自足、校準回報（對照工具輸出核對每句進度聲明——官方實測此指令「幾乎消除了捏造的進度回報」）、足夠即行動、不以承諾結束 turn、破壞性動作前的證據檢查、評估模式不擅自動手、範圍紀律、註解只寫約束。

另一個佐證來自反方向：Fable 5 上線時 Claude Code 把 system prompt 砍掉約 80%，因為高檔模型「要更小的 system prompt」——硬規則對強模型是束縛，對中階模型卻是必需品。**同一份紀律，強模型靠訓練內化，中階模型靠明文規則補**，這正是你的制度目錄的設計哲學，也是 70 檔的定位。

### 第三層：判斷習慣（部分可移植）

「何時該停下來問」「失敗幾次該換方法」「信心夠不夠直接做」在強模型身上是內化直覺，在中階模型身上要外化成 rubric。你的 20-judgment.md 已是先例；70 檔的 B5（失敗兩次換方法）、C3（證據檢查）、E（結尾自檢）延續同一策略。

## 二、為什麼體感差距這麼大

單看每條規則都小，但它們作用在三種「稅」上，而稅是複利的：

- **閱讀稅**：結論先行＋可讀性，讓你每則回覆少讀一遍。
- **信任稅**：校準回報讓你不必重驗每個「完成」。回報可信 → 敢授權 → 迭代變快。
- **來回稅**：足夠即行動＋不以承諾結束，把三次來回壓成一次完成。

Opus 4.8 的失分大多不在「想不出來」（日常任務實測幾乎打平），而在這三種稅：報告像日誌、完成宣告摻水、能自查的問題丟回來問。這些是紀律問題，所以可移植。Tessl 還有一個數字直接支持這個方向：**一個好的 agent skill 讓成績加 17 分，模型升級只加不到 1 分**——在日常任務上，harness 工程的槓桿比換模型大一個量級。

## 三、可移植性評估（附證據）

| 行為 | 移植度 | 證據 |
|---|---|---|
| 結論先行、可讀性、訊息自足 | 高 | 兩家官方文件都列為可指令調控項 |
| 校準回報、證據先於斷言 | 高 | 官方：audit-claims 指令幾乎消除捏造回報 |
| 不以承諾結束、自主推進 | 中高 | OpenAI Codex 指南有同型條款（bias to action） |
| 該問 vs 該做的判斷 | 中 | 靠 rubric 外化（20 檔＋70 檔 B3/B6） |
| 推理深度、長程一致性 | 低 | METR：任務時長上限是模型屬性；解法是拆任務不是加 prompt |
| 誠實度的「底層」 | 低 | 4.6% vs >50% 是訓練成果；中階模型須靠外部驗證補 |

移植時的四個硬限制（都已反映在 70 檔設計裡）：

1. **規則檔是建議不是強制**（官方原文：CLAUDE.md 是 context 不是 enforcement）——必須保證的事要用 hooks／permissions，不是規則。
2. **長度預算**：規則檔超過約 200 行，遵循率開始衰減、規則會被「弄丟」。70 檔全文 66 行。
3. **GPT 的字面遵循**：矛盾規則對 GPT-5 的傷害比對 Claude 大（它會花推理 token 試圖調和矛盾），所以 70 檔條款保持彼此無矛盾，並明確聲明從屬於全域規則。
4. **安靜失敗風險**：研究顯示弱模型接觸強模型的推理軌跡，吸收的是表面風格不是推理邏輯——契約會讓中階模型「看起來」像 Fable，但深層錯誤率不變。解法你已經有了：驗證不自驗（10 檔）＋各類產出最低驗證標準（20 檔 §5），這兩條與 70 檔搭配使用，缺一不可。

## 四、我為你建立了什麼

1. **[70-behavior-contract.md](70-behavior-contract.md)**：可執行的行為契約。結構：核心迴圈 → 溝通／行動／驗證／程式碼四契約（附正反例）→ 結尾自檢五題 → 各平台掛載與侷限。
2. **兩側路由**：`~/.claude/CLAUDE.md` 與 `~/.codex/AGENTS.md` 各加一行「session 第一個實質任務開工前讀 70 檔」（依 40-maintenance 備份於 `backups/`）。
3. **README 索引**：兩個新檔已加入檔案索引。

### Fable 5 時間用完之後的建議配置

- **日常預設用 Opus 4.8**（每元品質高約 69%）＋ 70 檔契約補紀律 ＋ 既有驗證制度補信任。
- **架構、卡關除錯、審查仲裁升檔**到當下可用的最高檔模型（10 檔既有規則）。
- **長程大任務拆小**：中階模型的任務時長上限較短，把工作切到它的上限以內，比任何 prompt 都有效。
- **退化就重讀**：發現輸出又像日誌，把 70 檔「結尾自檢」五題貼回對話。

## 五、誠實的侷限

1. 我對自身訓練細節沒有內部知識。第一層的描述全部來自公開資料（下方來源）；第二、三層來自我對自己運作方式的直接觀察，這部分才是本報告的獨有價值。
2. 契約無法讓中階模型做出 Fable 級的架構判斷；它治的是紀律稅，不是能力差。
3. 引用的第三方評測各有方法論侷限（Tessl 的情境偏日常任務），數字當方向看，不當精確值看。

## 來源

- 官方：[發布公告](https://www.anthropic.com/news/claude-fable-5-mythos-5)、[模型介紹](https://platform.claude.com/docs/en/about-claude/models/introducing-claude-fable-5-and-claude-mythos-5)、[Fable 5 prompting 指南](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5)（dense 預設風格、readability 指令、audit-claims 效果均出自此）、[Claude Code memory 文件](https://code.claude.com/docs/en/memory)、[best practices](https://code.claude.com/docs/en/best-practices)
- 第三方：[Tessl 917 情境實測](https://tessl.io/blog/claude-fable-5-vs-opus-48-the-mythos-hype-meets-reality/)、[Zvi 的 system card 分析](https://thezvi.substack.com/p/claude-fable-5-and-mythos-5-the-system)（4.6% 誠實度數據）、[Verdent 編程實測](https://www.verdent.ai/guides/claude-fable-5-vs-opus-4-8-coding)、[system prompt 砍 80% 報導](https://the-decoder.com/anthropic-says-it-cut-80-percent-of-claude-codes-system-prompt-because-fable-5-models-want-a-smaller-system-prompt/)
- 移植限制：[OpenAI GPT-5 prompting 指南](https://developers.openai.com/cookbook/examples/gpt-5/gpt-5_prompting_guide)、[Codex prompting 指南](https://developers.openai.com/cookbook/examples/gpt-5/codex_prompting_guide)、[AGENTS.md 慣例](https://developers.openai.com/codex/guides/agents-md)、[METR 任務時長研究](https://metr.org/blog/2025-03-19-measuring-ai-ability-to-complete-long-tasks/)、[Small Model Learnability Gap](https://arxiv.org/html/2502.12143v1)

## 變更紀錄
- 2026-07-08 建檔（Fable 5）
