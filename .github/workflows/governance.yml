name: Governance checks

on: [push, pull_request]

permissions:
  contents: read

jobs:
  governance:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    env:
      PYTHONDONTWRITEBYTECODE: '1'
      PYTHONUTF8: '1'  # Windows runner defaults stdout to cp1252, which cannot encode the CJK check output
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'
      - run: python setup/govcheck.py
      - run: python -m pip install -r setup/requirements-tui.txt
      - run: python -m unittest discover -s setup -p 'test_*.py'
