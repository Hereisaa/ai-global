#!/bin/bash
# Retired Stop entry point: Stop payloads cannot attribute dirty files to a turn.
# Task acceptance owns necessary checks, using the declared packageManager.
# Existing installations may still invoke this file; it never runs commands,
# blocks a reply, or requests fixes for unrelated worktree changes.
exit 0
